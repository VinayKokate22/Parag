# Learning about git

1. about brach protection
2. git featch
3. git reset
   - to unadd from the stage
4. git reset HEAD~1
   - to undo a commit

5) git log
6) git checkout -b branch
   - used to visit a branch
   - first use git featch
